{
  "$schema": "https://schema.railpack.com",
    "provider": "node",
  "steps": {
    "install": {
      "commands": ["corepack enable", "pnpm install"]
    },
    "build": {
      "inputs": [{ "step": "install" }]
    }
  },
  "deploy": {
    "startCommand": "bun start"
  }
}